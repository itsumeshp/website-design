# --- Database (local dev uses docker-compose Postgres) ---
DATABASE_URI=postgres://fexo:fexo@localhost:5432/fexo

# --- Payload ---
PAYLOAD_SECRET=change-me-openssl-rand-hex-32

# --- Site ---
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# --- Email (contact form via Resend; leave blank to save leads without email) ---
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=onboarding@resend.dev
